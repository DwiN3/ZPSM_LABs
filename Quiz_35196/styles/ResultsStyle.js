// ResultsStyle.js

import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  containerResults: {
    flex: 1,
    alignItems: 'center',
  },
  centeredText: {
    fontSize: 18,
    textAlign: 'center',
  },
});

export default styles;